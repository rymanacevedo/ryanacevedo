# Optional GitHub personal access token for issue reads and writes.
# When omitted, Sandcastle exports the token from the host `gh` login.
# Required repository permissions: Issues (read/write) and Metadata (read).
GH_TOKEN=
